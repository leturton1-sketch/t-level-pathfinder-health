export default function App() {
  return (
    <main className="app-shell">
      <section className="reset-panel" aria-labelledby="pathfinder-title">
        <p className="eyebrow">T-Level Pathfinder Health</p>
        <h1 id="pathfinder-title">Clean rebuild workspace</h1>
        <p>
          The previous front-end, graphics, models and media assets have been removed.
          This shell is intentionally minimal and ready for a fresh build.
        </p>
      </section>
    </main>
  );
}

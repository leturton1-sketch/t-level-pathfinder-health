{
  "name": "AppUser",
  "type": "object",
  "properties": {
    "username": {
      "type": "string",
      "title": "Username",
      "minLength": 3
    },
    "pin": {
      "type": "string",
      "title": "PIN",
      "description": "4-digit PIN (default 0000)"
    },
    "role": {
      "type": "string",
      "enum": [
        "super_admin",
        "admin",
        "tutor",
        "student",
        "guest"
      ],
      "default": "student"
    },
    "title": {
      "type": "string",
      "title": "Custom Role Title",
      "description": "Optional override shown on the user's role badge (e.g. 'System Architect and T-Level Lead Clinical Educator')"
    },
    "full_name": {
      "type": "string",
      "title": "Full Name"
    },
    "institution": {
      "type": "string",
      "title": "Institution"
    },
    "cohort": {
      "type": "string",
      "title": "Cohort"
    },
    "first_login": {
      "type": "boolean",
      "default": true
    },
    "active": {
      "type": "boolean",
      "default": true
    },
    "ai_voice": {
      "type": "string",
      "enum": [
        "river",
        "honey",
        "sunny",
        "storm",
        "spark"
      ],
      "default": "honey"
    },
    "ai_persona": {
      "type": "string",
      "enum": [
        "male",
        "female"
      ],
      "default": "female"
    },
    "is_protected": {
      "type": "boolean",
      "default": false,
      "description": "System Architect account cannot be deleted"
    },
    "qr_token": {
      "type": "string",
      "title": "QR Login Token",
      "description": "Unique random token embedded in the user's Industry Academy ID card QR code, used to sign in without a username/PIN"
    }
  },
  "required": [
    "username",
    "pin",
    "role",
    "full_name"
  ]
}